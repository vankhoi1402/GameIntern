using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>Script placeholder trống — chưa dùng trong gameplay, có thể xóa.</summary>
public class NewBehaviourScript : MonoBehaviour
{
    /// <summary>Unity lifecycle — chưa có logic.</summary>
    void Start()
    {
    }

    /// <summary>Unity lifecycle — chưa có logic.</summary>
    void Update()
    {
    }
}
